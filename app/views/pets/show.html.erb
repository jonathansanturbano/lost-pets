<h1><%= @pet.name %></h1>
<p>Species: <%= @pet.species %></p>
<p>Address: <%= @pet.address %></p>
<p>Found on: <%= @pet.found_on %></p>

<%= link_to "Edit post about #{@pet.name}", edit_pet_path(@pet), class: "btn btn-info" %>

<%= link_to "Destroy post about #{@pet.name}", pet_path(@pet), method: :delete, data: { confirm: "Are you sure?"}, class: "btn btn-danger" %>

<%= link_to "See all lost pets", pets_path, class: "btn btn-primary" %>
